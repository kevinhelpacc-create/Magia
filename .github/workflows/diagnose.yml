name: Diagnose runners

on:
  workflow_dispatch:

jobs:
  linux-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ubuntu runner OK"

  macos-check:
    runs-on: macos-latest
    steps:
      - run: echo "macos runner OK"
